<system-notice>
User explicitly requested the document subagent.

MUST delegate the document question through `task` with `agent: "doc-researcher"` and wait for its result.
NEVER substitute `librarian`, `scout`, or the default agent. `doc-researcher` is manual-only and intentionally absent from the permanent agent catalog.
Pass the actual question as the assignment; omit the routing phrase when it adds no research meaning. Return the agent's evidence-backed answer and citations.
</system-notice>
